#include "stop.h"
#include <iostream>
/*
Stop::Stop(std::string_view name_): name(name_){
}

void Stop::Read(std::istream& in) {
	in >> this->point.latitude;
	in.ignore(1);
	in >> this->point.longitude;
}*/

